name: Windows 실행파일 빌드

on:
  workflow_dispatch:  # 수동 실행만

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: finsupport_final

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: finsupport_final/requirements.txt

      - name: 의존성 설치
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: PyInstaller 빌드
        run: |
          pyinstaller --onefile --windowed --name="FinsupportManager" --add-data="code_map.py;." --add-data="utils.py;." --hidden-import=supabase --hidden-import=gotrue --hidden-import=postgrest --hidden-import=realtime --hidden-import=storage3 --hidden-import=supafunc --hidden-import=pandas --hidden-import=openpyxl --hidden-import=beautifulsoup4 --hidden-import=tkinter --hidden-import=tkinter.ttk --hidden-import=tkinter.messagebox --hidden-import=customtkinter --hidden-import=aiohttp --hidden-import=threading --hidden-import=asyncio team_manager_modern.py

      - name: 빌드 결과 업로드
        uses: actions/upload-artifact@v4
        with:
          name: FinsupportManager-Windows
          path: finsupport_final/dist/FinsupportManager.exe
          retention-days: 30
